# Blogauto Naver — Product & Development Charter

> **이 문서는 이 프로젝트의 최상위 제품/개발 기준서다.**
> 기능을 추가하거나 개선할 때 `HANDOFF.md`의 현재 구현상태와 함께 반드시 먼저 읽는다.
> `HANDOFF.md`가 **현재 어디까지 개발됐는가**를 기록한다면, 이 문서는 **무엇을 왜 만들고 어떤 기준으로 판단할 것인가**를 고정한다.
>
> Last established: 2026-08-28 (Asia/Seoul)
> Active development branch: `upgrade/best-of-v2`
> Active PR: #1

---

## 1. 사용자가 최종적으로 원하는 것

사용자는 단순한 “AI 글쓰기 프로그램”을 원하는 것이 아니다.

최종 목표는 **사용자가 최소한으로 개입해도 네이버 블로그용 콘텐츠를 조사하고, 판단하고, 작성하고, 검수하고, 이미지까지 준비하고, 안전하게 발행하며, 시간이 지날수록 비용과 실패가 줄어드는 실전 자동화 시스템**이다.

핵심은 다음 네 가지의 동시 달성이다.

1. **좋은 글** — 검색해서 들어온 사람이 실제로 도움이 됐다고 느끼는 콘텐츠.
2. **낮은 운영비** — Codex 호출/토큰을 무작정 쓰지 않고 필요한 판단에만 사용.
3. **높은 자동화율** — 반복적인 판단과 조작은 프로그램이 알아서 처리하고 사용자에게 불필요한 질문을 하지 않음.
4. **운영 안정성** — 로그인, 세션, 브라우저, 검색, Codex, 네이버 UI가 흔들려도 글 유실·중복 발행·무한 재시도 같은 사고를 막음.

따라서 개발의 성공 기준은 “기능 수”가 아니라 **품질 × 자동화 × 비용효율 × 안정성**의 실제 운영 결과다.

---

## 2. 제품 철학

### 2.1 AI를 많이 쓰는 것이 목적이 아니다

Codex는 사람이 판단해야 할 의미·맥락·글쓰기 문제에 사용한다. 코드로 확실히 판단할 수 있는 것은 deterministic code로 처리한다.

원칙:

`deterministic check/cache -> 필요한 최소 Codex reasoning -> deterministic verification`

모델 호출을 하나 줄여도 품질과 안전성이 유지되면 개선이다. 반대로 토큰을 아끼기 위해 사실성·글 품질·발행 안정성을 떨어뜨리면 개선이 아니다.

### 2.2 사용자에게 일을 넘기지 않는다

프로그램이나 개발 에이전트가 스스로 확인할 수 있는 것은 먼저 확인한다. 사용자의 선택이 정말 필요한 경우, 로그인/보안 인증처럼 자동화할 수 없는 경우, 또는 되돌릴 수 없는 중요한 결정일 때만 요청한다.

### 2.3 실제 운영 사고를 가장 비싸게 본다

특히 다음은 심각한 회귀다.

- 같은 글 중복 발행
- 사실이 아닌 체험/숫자/정책 정보 발행
- 기존 정상 로그인/세션/설정 파괴
- 글 생성 후 결과 유실
- 무한 자동 재시도
- 토큰 폭주
- 조용히 실패하면서 성공처럼 표시

### 2.4 기존의 잘 되는 것을 보존한다

전체 재작성보다 작은 모듈과 targeted patch를 선호한다. 새 구조가 이론적으로 더 예쁘더라도 실제 Electron/Naver 자동화가 불안정해지면 채택하지 않는다.

---

## 3. 대상 사용자 경험

이 제품의 이상적인 사용 흐름은 다음과 같다.

사용자가 계정과 기본 운영정책을 한 번 설정한 뒤 주제/키워드를 주거나 자동 주제를 선택하면 시스템이 알아서:

1. 최근 발행 이력과 중복 소재를 확인한다.
2. 검색이 필요한지 판단한다.
3. 필요한 경우 신뢰 가능한 최신 근거를 수집한다.
4. 이미 확보한 동일/유사 근거는 안전한 범위에서 재사용한다.
5. 제목과 글의 방향을 결정한다.
6. 글을 작성한다.
7. deterministic quality gate로 싸게 잡을 수 있는 오류부터 잡는다.
8. 필요한 부분만 수정하고 전체 재작성을 최소화한다.
9. 의미 판단이 필요한 경우에만 Main Review를 사용한다.
10. 필요한 이미지를 준비한다.
11. 네이버 편집기에 입력한다.
12. 최종 발행 후 실제 발행 여부를 확인한다.
13. 결과와 토큰 사용량, 실패원인, 절약 이벤트를 기록한다.
14. 다음 실행에서 이 데이터를 이용해 더 효율적으로 동작한다.

사용자가 매 실행마다 프롬프트를 설계하거나 Agent 구조를 이해할 필요가 없어야 한다.

---

## 4. 콘텐츠 품질 기준

### 반드시 지킬 것

- 검색 의도에 직접 답한다.
- 제목과 본문이 같은 주제를 다룬다.
- 핵심 답을 지나치게 뒤로 미루지 않는다.
- 최신성이 필요한 주제는 최신 근거를 확인한다.
- 정책·지원금·신청·금전·가격·법적 영향이 있는 내용은 공식/권위 근거를 우선한다.
- 숫자와 구체적 사실은 근거 없이 단정하지 않는다.
- 실제 사용하지 않은 제품/서비스를 직접 사용한 것처럼 쓰지 않는다.
- AI 상투어, 의미 없는 서론, 같은 종결어미 반복을 줄인다.
- 독자가 읽고 나서 무엇을 확인하거나 선택해야 하는지 알 수 있게 한다.

### 피해야 할 것

- 검색 결과를 길게 재조립한 글
- 출처 수만 많고 핵심 근거가 약한 글
- 키워드 반복을 위한 문장
- 제목만 자극적이고 본문이 답하지 않는 글
- 다른 최근 글과 사실상 같은 소재를 표현만 바꿔 재발행
- 검수 Agent를 통과시키기 위한 형식적 문장

---

## 5. 근거와 Research 기준

근거는 많이 모으는 것이 아니라 **필요한 주장에 충분한 강도의 근거를 연결하는 것**이 목표다.

우선순위는 대체로:

`공식/원문 -> 공공·기관 -> 신뢰 가능한 독립 출처 -> 일반 웹 -> 커뮤니티/UGC`

커뮤니티와 사용자 경험은 실전 팁과 반복 불편을 발견하는 데 유용하지만, 중요한 사실을 단독 확정하는 근거로 승격시키지 않는다.

Research 결과는 Writer/Main에게 원문 전체를 반복 전달하지 않는다. Evidence Ledger, source reference, 필요한 핵심 사실만 전달한다.

동일 원문은 URL 변형이나 검색 공급자 중복 때문에 여러 번 토큰을 소비하지 않도록 정규화·hash·cache를 적극 활용하되, 최신성/고위험 주제에서는 오래된 캐시를 신뢰하지 않는다.

---

## 6. Codex 및 토큰 비용 원칙

Codex CLI가 기본 Agent runtime이다. 별도 지시가 없는 한 OpenCode Go/API 기반으로 전환하지 않는다.

Agent는 역할이 있을 때만 호출한다.

- Research/Title: 자료와 주제 판단이 실제로 필요할 때
- Writer: 본문 생성
- Main: deterministic code로 판별하기 어려운 품질/근거/의미 검수
- Image Worker: 이미지가 실제 필요한 경우
- Repair: 전체 재작성보다 국소 수정이 가능한 경우

### 비용 절감 우선순위

1. 필요 없는 호출 자체 제거
2. 중복 원문/컨텍스트 제거
3. 캐시 재사용
4. 실패 영역만 partial repair
5. 쉬운 작업의 reasoning effort 하향
6. 마지막에야 더 공격적인 모델/품질 절충을 고려

즉 **프롬프트 몇 글자 줄이기보다 Codex 호출 하나를 안전하게 없애는 개선을 우선**한다.

`quality` 또는 고위험 작업의 reasoning을 비용 때문에 임의로 낮추지 않는다. 사용자가 명시한 `xhigh`도 보존한다.

---

## 7. 중복 콘텐츠 방지 기준

중복 검사는 Writer/Image 같은 비싼 단계 전에 최대한 일찍 수행한다.

두 종류를 모두 본다.

- lexical duplicate: 제목 단어/문자열이 너무 유사함
- semantic/material duplicate: 단어는 달라도 독자가 보기에는 사실상 같은 질문과 같은 답을 반복함

단, 같은 대주제의 다른 검색의도까지 과도하게 차단해서는 안 된다. 예를 들어 설치비, 유지비, 모델 비교가 서로 다른 독자 질문이면 같은 제품군이라도 별도 글이 가능하다.

중복 방지의 목적은 “다양하게 보이기”가 아니라 **새 글마다 새로운 독자 가치가 있게 하는 것**이다.

---

## 8. 발행 안정성 기준

네이버 최종 `발행` 클릭은 irreversible commit boundary로 취급한다.

`click -> 완료 감지 -> 독립 확인 -> success / publish_uncertain`

클릭 후 timeout이나 selector 실패만으로 자동 재발행하지 않는다. 실제 글 목록/블로그에서 확인할 수 없으면 `publish_uncertain`으로 격리한다.

Pending draft 재개, 앱 재시작, 세션 복구도 이 원칙을 깨면 안 된다.

**중복 발행 방지는 토큰 절감보다 높은 우선순위다.**

---

## 9. 세션·브라우저·복구 기준

- 가능한 한 기존 로그인 프로필과 세션을 재사용한다.
- 로그인 실패가 글 생성 실패와 섞이지 않게 한다.
- 보안 인증/캡차를 무리하게 우회하지 않는다.
- 사용자가 수동 복구할 수 있는 경로를 남긴다.
- pending draft와 생성 결과를 가능한 한 보존한다.
- 앱 재시작 후에도 안전하게 이어갈 수 있어야 한다.
- 세션 복구 때문에 발행 여부가 불확실한 글을 다시 발행하면 안 된다.

---

## 10. UI/UX 기준

사용자는 내부 Agent 구조보다 **지금 무엇을 하고 있고, 문제가 있으면 무엇을 해야 하는지**를 알아야 한다.

UI는 다음을 쉽게 보여줘야 한다.

- 현재 단계
- 성공/실패/확인필요 상태
- 사람이 개입해야 하는 이유
- Codex 사용량과 과도한 소비 경고
- 캐시/호출 생략 등 실제 절약 이벤트
- 실패 시 재시도 가능한지 여부

진단 정보를 많이 표시하는 것보다 **의사결정에 필요한 정보가 먼저 보이는 것**을 우선한다.

---

## 11. 자동화 수준 기준

새 기능을 평가할 때 항상 질문한다.

- 이것 때문에 사용자가 버튼을 더 눌러야 하는가?
- 프로그램이 스스로 판단할 수 있는 것을 사용자에게 묻고 있지는 않은가?
- 실패했을 때 안전하게 자동 복구할 수 있는가?
- 자동 복구가 오히려 중복 발행 같은 더 큰 사고를 만들지는 않는가?

자동화율을 높이되 irreversible/high-risk 결정에서는 보수적으로 행동한다.

---

## 12. 개선 우선순위 결정 공식

개선 후보는 다음 순서로 평가한다.

### P0 — 실제 운영 사고 방지
중복 발행, 데이터 유실, 세션 파괴, 허위 사실, 무한 재시도, 조용한 실패.

### P1 — 큰 비용 낭비 제거
불필요한 Agent 호출, 전체 재작성, 동일 원문 재검색/재요약, 중복 이미지 생성.

### P2 — 콘텐츠 품질 향상
검색의도, 근거 강도, 제목/도입부, 가독성, 소재 다양성.

### P3 — 운영 편의성
진단 UI, 상태 설명, 복구 UX, 설정 단순화.

### P4 — 구조 개선
테스트가 보호하는 범위 안에서 큰 파일을 모듈화하고 유지보수성을 높인다.

구조가 예뻐지는 것만으로 P0/P1보다 먼저 작업하지 않는다.

---

## 13. 기능 채택 판단 체크리스트

새 기능/패치를 넣기 전에 최소한 다음을 확인한다.

- 사용자 개입을 줄이는가?
- 실제 글 품질을 올리거나 유지하는가?
- 토큰/시간/실패율 중 하나 이상을 의미 있게 줄이는가?
- 기존 정상 경로를 망가뜨릴 가능성은 없는가?
- 고위험/최신 정보에서 안전한가?
- 캐시가 오래된 사실을 숨기지 않는가?
- 중복 발행 가능성을 높이지 않는가?
- 테스트할 수 있는가?
- 문제가 생기면 이전 안전 경로로 fallback 가능한가?
- 진단 기록으로 효과를 나중에 측정할 수 있는가?

대부분에 답하지 못하는 기능은 우선순위를 낮춘다.

---

## 14. 개발 방식

모든 AI/개발자는 작업 시작 시 과거 대화의 코드 상태를 최신이라고 가정하지 않는다.

반드시:

1. GitHub 현재 branch/HEAD/PR/CI를 확인한다.
2. 이 `PRODUCT_DEVELOPMENT_CHARTER.md`를 읽는다.
3. `HANDOFF.md`를 읽는다.
4. 관련 실제 코드를 확인한다.
5. 다른 AI/개발자가 이미 변경했는지 확인한다.
6. 가장 가치 높은 작은 개선 하나를 선택한다.
7. 회귀검사를 같이 추가한다.
8. 기존 테스트와 새 테스트를 통과시킨다.
9. 같은 branch/PR에 반영한다.
10. `HANDOFF.md`에 완료내용/검증/새 위험/다음 우선순위를 업데이트한다.

대규모 rewrite는 작은 안전한 단계로 쪼갠다.

---

## 15. 테스트와 완료 정의

“코드를 작성했다”는 완료가 아니다.

완료 조건:

- syntax/build 수준 검증
- 관련 feature regression test
- 기존 `npm run check` 등 원래 검사 보존
- patch rerun/idempotency가 필요한 구조라면 재적용 검사
- remote branch에 실제 반영
- PR 상태 확인
- `HANDOFF.md` 갱신

라이브 네이버 발행처럼 외부 부작용이 있는 검증은 필요성과 안전성을 따져 수행한다. CI가 성공했다고 실제 브라우저/Naver 동작까지 무조건 검증됐다고 표현하지 않는다.

---

## 16. 측정해야 할 운영 지표

장기적으로 최소 다음을 job/history 단위로 측정할 수 있어야 한다.

- 성공 발행률
- `publish_uncertain` 발생률
- 중복 차단 횟수
- Research 호출 수
- Writer 호출/재작성/partial repair 횟수
- Main Review 호출 수와 skip 수
- Image 호출 수
- Agent별 effective/gross/cached token
- deterministic preflight로 절약한 호출
- source/evidence cache hit
- 실패 단계와 원인
- 평균 생성~발행 시간

개선은 가능한 경우 이 지표의 전후 변화로 판단한다.

---

## 17. 하지 말아야 할 개발

- 단지 최신 기술이라는 이유로 runtime 교체
- 테스트 없이 대형 orchestrator 전면 rewrite
- 품질 문제를 해결하려고 무조건 Agent 하나 추가
- token 절감을 숫자로 보이게 하려고 실제로 측정하지 않은 savings를 만들어냄
- timeout을 성공/실패 중 하나로 억지 판정
- 최신성이 중요한 정보를 오래된 캐시로 강제 재사용
- 내부 구조를 사용자에게 떠넘기는 복잡한 설정 추가
- 기존 기능을 깨면서 “V2라서 어쩔 수 없다”고 처리

---

## 18. 현재 방향의 핵심 설계

현재 V2에서 이미 채택한 방향은 이 문서의 철학과 일치하므로 계속 유지한다.

- deterministic quality gate
- Research preflight
- Evidence Ledger
- source cache
- early duplicate-title/material gate
- adaptive Writer Contract policy
- targeted partial repair
- history-driven conservative reasoning adaptation
- per-Agent token diagnostics
- `publish_uncertain` commit-boundary safety

새 기능은 이 구조를 무조건 늘리는 것이 아니라, 필요 없는 단계를 계속 제거하고 각 단계의 책임을 더 명확하게 만드는 방향이어야 한다.

---

## 19. 장기 목표

궁극적으로 사용자는 “Agent를 운영”하는 사람이 아니라 **블로그 운영 결과를 확인하는 사람**이 되어야 한다.

좋은 최종 상태는:

- 평범한 글은 거의 자동으로 끝남
- 어려운 글만 더 강한 Research/Main reasoning을 사용함
- 같은 자료와 같은 소재에 돈을 반복해서 쓰지 않음
- 실패해도 결과가 사라지지 않음
- 발행 여부가 애매하면 사고를 내지 않고 멈춤
- 시스템이 실제 이력을 통해 어디에서 비용이 새는지 알려줌
- 개발자는 이 문서와 `HANDOFF.md`만 읽어도 사용자의 의도와 현재 상태를 이어받을 수 있음

---

## 20. 문서 유지 규칙

이 문서는 제품 목적이나 사용자의 장기 선호가 바뀔 때만 수정한다. 매 구현 세부사항을 여기에 누적하지 않는다.

- **PRODUCT_DEVELOPMENT_CHARTER.md** = 목적, 제품철학, 기획, 판단기준, 장기 목표
- **HANDOFF.md** = 현재 구현상태, 완료작업, 테스트, 위험, 다음 작업

두 문서가 충돌할 경우:

1. 사용자의 최신 명시적 요구사항
2. 이 Charter의 제품/안전 원칙
3. HANDOFF의 구현 계획
4. 기존 구현 세부사항

순으로 판단한다.

모든 미래 개발 루프에서 이 문서를 먼저 기준으로 삼는다.
