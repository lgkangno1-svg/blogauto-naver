name: V2 Core Upgrade

on:
  workflow_dispatch:
  push:
    branches:
      - upgrade/best-of-v2
    paths:
      - "src/lib/qualityGate.js"
      - "src/lib/tokenPolicy.js"
      - "scripts/apply-v2-core.js"
      - ".github/workflows/v2-core-upgrade.yml"

permissions:
  contents: write

jobs:
  upgrade:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: upgrade/best-of-v2
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Apply idempotent V2 core patch
        run: node scripts/apply-v2-core.js

      - name: Syntax check upgraded modules
        run: |
          node --check src/lib/qualityGate.js
          node --check src/lib/tokenPolicy.js
          node --check src/lib/codexRunner.js

      - name: Run repository checks
        run: npm run check

      - name: Commit generated core integration
        shell: bash
        run: |
          if git diff --quiet; then
            echo "No generated patch changes"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/lib/codexRunner.js
          git commit -m "feat: integrate V2 quality and token policies"
          git push origin HEAD:upgrade/best-of-v2
